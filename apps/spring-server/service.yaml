apiVersion: v1
kind: Service
metadata:
  name: spring-server
  namespace: sikd
  labels:
    app: spring-server
spec:
  selector:
    app: spring-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  
