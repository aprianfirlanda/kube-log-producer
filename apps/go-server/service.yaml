apiVersion: v1
kind: Service
metadata:
  name: go-server
  namespace: sikd
  labels:
    app: go-server
spec:
  selector:
    app: go-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  
